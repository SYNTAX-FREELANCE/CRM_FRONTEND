// src/components/forms/SectionHeader.jsx
import { styles } from '../../Style/formStyles';

const SectionHeader = ({ title }) => (
    <div style={styles.sectionHeader}>{title}</div>
);

export default SectionHeader;